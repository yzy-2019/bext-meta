import {
  alook_install,
  bz_install,
  lit_install,
  lit_installed,
  shark_install,
  shark_installed,
  via_install,
  via_installed,
  via_uninstall,
  x_install,
  x_installed,
  x_uninstall,
} from '@bext/browser';
import { buildMethods } from '@bext/browser';

export const browser = buildMethods({
  alook_install,
  bz_install,
  lit_install,
  lit_installed,
  shark_install,
  shark_installed,
  via_install,
  via_installed,
  via_uninstall,
  x_install,
  x_installed,
  x_uninstall,
});
